[project]
name = "seian-mesh-simulator"
version = "0.4.0"
description = "SEIAN Adaptive LoRa Mesh Simulator"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
